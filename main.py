def exponentiation(number, degree):
  
  # Код функции возведения в степень.
  # Нельзя использовать выражение вида number**degree. 
  # В теле функции следует использовать переменные number и degree,
  # где number -- основание, degree -- степень, в которое возводится основание.
  # Результат функции -- переменная result.

  # ПРИМЕЧАНИЕ: Функция должна включать в себя ТОЛЬКО целочисленные значения int (-32768 ... 32767).
  
  return result

a = int(input("number = "))
b = int(input("degree = "))

print(exponentiation(a, b))
